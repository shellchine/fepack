var fs = require('fs');
var ftp = require('../lib/ftp');
var util = require("../lib/util4go");
var vc = util.vc;
var htmlDir = util.distHtmlDir;
var log = util.log;

var scp = {
    
}

scp.publish = function(){
    
}

module.exports = scp;
